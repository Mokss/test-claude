{
  "name": "@ismart/core-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "typeCheck": "tsc --noEmit",
    "start": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@ismart/specs": "*",
    "@fastify/jwt": "^9.0.0",
    "fastify": "^5.0.0",
    "mongodb": "^6.0.0",
    "amqplib": "^0.10.4",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
